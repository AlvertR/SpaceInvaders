namespace SpaceInvaders
{
    public enum GameStatus
    {
        Start,
        Playing,
        Paused,
        GameOver,
        End,
    }

    public enum ShotStatus
    {
        Active,
        Impact,
    }
}
