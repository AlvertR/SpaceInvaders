namespace SpaceInvaders
{
    public enum GameStatus
    {
        Start,
        Playing,
        Paused,
        GameOver,
        End,
    }

    public enum ShotStatus
    {
        Active,
        Impact,
    }

    public enum EnemyStatsu
    {
        Pasive,
        Active,
        Dead,
        Out,
    }
}
